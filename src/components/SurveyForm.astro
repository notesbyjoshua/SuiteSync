---
const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL ?? '';
const supabaseKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY ?? '';
---

<div class="survey-shell" data-survey-root data-supabase-url={supabaseUrl} data-supabase-key={supabaseKey}>
  <header class="survey-intro">
    <a class="back-link" href={import.meta.env.BASE_URL}>← SuiteSync home</a>
    <span class="survey-kicker">YYGS RESIDENTIAL LIFE</span>
    <h1>Let’s find your suite.</h1>
  </header>

  <div class="testing-banner"><strong>Testing mode</strong><span>Sign-in is temporarily disabled. Please use test information only.</span></div>

  <div class="survey-layout" data-survey-layout>
    <aside class="survey-progress" aria-label="Survey progress">
      <div class="progress-heading">
        <span>YOUR PROGRESS</span>
        <strong data-progress-label>1 of 6</strong>
      </div>
      <div class="progress-track" aria-hidden="true"><span data-progress-bar></span></div>
      <ol>
        <li class="is-active" data-progress-step="0"><span>1</span>Welcome</li>
        <li data-progress-step="1"><span>2</span>About you</li>
        <li data-progress-step="2"><span>3</span>Personality</li>
        <li data-progress-step="3"><span>4</span>Room preferences</li>
        <li data-progress-step="4"><span>5</span>Location</li>
        <li data-progress-step="5"><span>6</span>Review</li>
      </ol>
      <p>Your responses are private and are used only for suite matching and residential planning.</p>
    </aside>

    <form class="survey-card" data-survey-form novalidate>
      <section class="survey-section is-active" data-section="0">
        <span class="step-label">STEP 1 OF 6</span>
        <h2>Welcome to YYGS!</h2>
        <div class="welcome-copy">
          <p>YYGS Residential offers a rare opportunity for like-minded students from around the world to meet at Yale’s historic campus. Students spend two weeks living in college dormitories, eating at Yale dining halls, and learning in an environment that encourages student collaboration and diverse, global perspectives.</p>

          <p>Summer 2027 YYGS will be hosted at Benjamin Franklin College and Pauli Murray College, two of Yale’s newest residential colleges.</p>

          <h3>Life in the residential colleges</h3>
          <p>Students will enjoy access to a wide range of on-site amenities, including laundry rooms, butteries, music practice rooms, large courtyards, a prayer room, and libraries ideal for both studying and collaboration.</p>

          <p>Each student room is furnished with a twin XL bed, desk, chair, and wardrobe or closet. Shared communal bathrooms are located throughout each residential area. YYGS housing is assigned by gender identity, and we are proud to offer three housing options to support student comfort and inclusion: male, female, and gender-inclusive housing.</p>

          <h3>Suites and rooms</h3>
          <p>Students will be assigned to suites of 4–6 people, or to a single dorm room if preferred. If you are in a suite, you and your suitemates will share a large common room, with 3–4 single or double dorm rooms in each suite.</p>

          <h3>Before completing this survey</h3>
          <p>Please fill out the following survey to share your roommate preferences. Requests will be prioritized on a first-come, first-served basis. Failure to complete this form by February 28, 2027 <em>(date subject to confirmation)</em> will result in random assignment.</p>

          <p>Any request for a room with a member of the opposite biological sex, or any response suggesting segregation toward a specific group of people, will cause the survey to be disregarded and the student to be assigned randomly.</p>

          <p>If an answer or request is flagged as concerning by the matching algorithm, the student may be asked to speak personally with the program manager after arrival and before check-in. Misspelled or unrecognized answers may be treated as “no preference” for that question.</p>
        </div>
        <div class="info-panel">
          <strong>Before you begin</strong>
          <ul>
            <li>Answer for how you actually live, not how you think you should live.</li>
            <li>Matching considers the whole group; no single answer determines your suite.</li>
            <li>Accessibility and accommodation requests should still go through official YYGS channels.</li>
          </ul>
        </div>
        <label class="check-field">
          <input type="checkbox" name="intro_acknowledged" required />
          <span>I understand how my responses will be used.</span>
        </label>
      </section>

      <section class="survey-section" data-section="1" hidden>
        <span class="step-label">STEP 2 OF 6</span>
        <h2>First, tell us who you are.</h2>
        <p class="section-copy">We’ll use these details to connect this response to your YYGS application.</p>
        <div class="field-grid personal-grid">
          <label class="field"><span>Preferred name</span><input name="preferred_name" autocomplete="given-name" required maxlength="80" placeholder="What should your suitemates call you?" /></label>
          <label class="field"><span>Application email</span><input name="email" type="email" autocomplete="email" required maxlength="254" placeholder="you@example.com" /></label>
          <label class="field field-full"><span>YYGS track</span><select name="track" required><option value="">Select a track</option><option value="Innovations in Science & Technology">Innovations in Science & Technology</option><option value="Politics, Law & Economics">Politics, Law & Economics</option><option value="Solving Global Challenges">Solving Global Challenges</option></select></label>
          <label class="field"><span>YYGS session</span><select name="session" required><option value="">Select a session</option><option value="Session I">Session I</option><option value="Session II">Session II</option><option value="Session III">Session III</option></select></label>
          <label class="field"><span>How old are you?</span><input name="age" type="number" min="15" max="18" required placeholder="16" /><small>You will be given a roommate within two years of your age unless specified otherwise.</small></label>
        </div>
        <fieldset class="question-block compact-options">
          <legend>What is your biological sex?</legend>
          <div class="choice-grid">
            <label class="choice-chip"><input type="radio" name="biological_sex" value="male" required /><span>Male</span></label>
            <label class="choice-chip"><input type="radio" name="biological_sex" value="female" /><span>Female</span></label>
            <label class="choice-chip"><input type="radio" name="biological_sex" value="non_binary" /><span>Non-binary</span></label>
            <label class="choice-chip"><input type="radio" name="biological_sex" value="other" /><span>Other</span></label>
          </div>
        </fieldset>
        <fieldset class="question-block compact-options">
          <legend>What are your pronouns?</legend>
          <div class="choice-grid">
            <label class="choice-chip"><input type="radio" name="pronouns" value="he/him" required /><span>He/him</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="he/they" /><span>He/they</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="she/her" /><span>She/her</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="she/they" /><span>She/they</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="they/them" /><span>They/them</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="other" /><span>Other</span></label>
            <label class="choice-chip"><input type="radio" name="pronouns" value="prefer_not_to_say" /><span>Prefer not to say</span></label>
          </div>
        </fieldset>
        <fieldset class="question-block compact-options">
          <legend>What is your gender identity?</legend>
          <div class="choice-grid">
            <label class="choice-chip"><input type="radio" name="gender_identity" value="male" required /><span>Male</span></label>
            <label class="choice-chip"><input type="radio" name="gender_identity" value="female" /><span>Female</span></label>
            <label class="choice-chip"><input type="radio" name="gender_identity" value="other" /><span>Other</span></label>
            <label class="choice-chip"><input type="radio" name="gender_identity" value="prefer_not_to_say" /><span>Prefer not to say</span></label>
          </div>
        </fieldset>
        <fieldset class="question-block compact-options">
          <legend>What is your ethnicity?</legend>
          <div class="choice-grid">
            <label class="choice-chip"><input type="radio" name="ethnicity" value="white" required /><span>White</span></label>
            <label class="choice-chip"><input type="radio" name="ethnicity" value="black" /><span>Black</span></label>
            <label class="choice-chip"><input type="radio" name="ethnicity" value="east_asian" /><span>East Asian</span></label>
            <label class="choice-chip"><input type="radio" name="ethnicity" value="other" /><span>Other</span></label>
          </div>
        </fieldset>
        <label class="field"><span>Do you have a specific religious belief? <small>(optional)</small></span><select name="religious_belief"><option value="">Select an option</option><option value="christian">Christian</option><option value="muslim">Muslim</option><option value="hindu">Hindu</option><option value="sikh">Sikh</option><option value="jewish">Jewish</option><option value="buddhist">Buddhist</option><option value="atheist">Atheist</option><option value="non_denomination">Non-denominational</option><option value="agnostic">Agnostic</option><option value="other">Other</option><option value="prefer_not_to_say">Prefer not to say</option></select></label>
      </section>

      <section class="survey-section" data-section="2" hidden>
        <span class="step-label">STEP 3 OF 6 · PERSONALITY INFORMATION</span>
        <h2>Tell us about your personality.</h2>
        <p class="section-copy">Choose the point on each scale that feels most accurate for you.</p>
        <label class="range-field scored-range"><span><strong>How extroverted are you?</strong><output data-range-output="extroversion">3 — balanced</output></span><input type="range" name="extroversion" min="1" max="5" value="3" /><span class="range-ends"><small>1 — Very introverted</small><small>5 — Very extroverted</small></span></label>
        <label class="range-field scored-range"><span><strong>How organized are you?</strong><output data-range-output="organization">3 — moderately organized</output></span><input type="range" name="organization" min="1" max="5" value="3" /><span class="range-ends"><small>1 — Very relaxed</small><small>5 — Very organized</small></span></label>
      </section>

      <section class="survey-section" data-section="3" hidden>
        <span class="step-label">STEP 4 OF 6 · ROOMMATE PREFERENCES</span>
        <h2>What room arrangement works for you?</h2>
        <fieldset class="question-block compact-options"><legend>Do you prefer a single or double room?</legend><div class="choice-grid three-options"><label class="choice-chip"><input type="radio" name="room_type" value="single" required /><span>Single room<br />(no roommate)</span></label><label class="choice-chip"><input type="radio" name="room_type" value="double" /><span>Double room<br />(with roommate)</span></label><label class="choice-chip"><input type="radio" name="room_type" value="no_preference" /><span>I don’t care</span></label></div></fieldset>
        <label class="field"><span>What time do you usually go to sleep?</span><select name="bedtime_preference" required><option value="">Select a usual bedtime</option><option value="21:00">9:00 PM</option><option value="21:30">9:30 PM</option><option value="22:00">10:00 PM</option><option value="22:30">10:30 PM</option><option value="23:00">11:00 PM</option><option value="23:30">11:30 PM</option><option value="00:00_or_later">12:00 AM or later</option></select></label>
        <label class="field"><span>How many suitemates do you prefer?</span><input name="preferred_suitemates" type="number" min="0" max="8" required placeholder="Choose from 0 to 8" /></label>
      </section>

      <section class="survey-section" data-section="4" hidden>
        <span class="step-label">STEP 5 OF 6 · LOCATION PREFERENCES</span>
        <h2>Where would you feel most comfortable?</h2>
        <fieldset class="question-block compact-options"><legend>Do you prefer a higher or lower floor?</legend><div class="choice-grid three-options"><label class="choice-chip"><input type="radio" name="floor_preference" value="higher" required /><span>Higher floor</span></label><label class="choice-chip"><input type="radio" name="floor_preference" value="lower" /><span>Lower floor</span></label><label class="choice-chip"><input type="radio" name="floor_preference" value="no_preference" /><span>No preference</span></label></div></fieldset>
        <fieldset class="question-block compact-options"><legend>Do you prefer a specific residential college?</legend><div class="choice-grid three-options"><label class="choice-chip"><input type="radio" name="college_preference" value="pauli_murray" required /><span>Pauli Murray College</span></label><label class="choice-chip"><input type="radio" name="college_preference" value="benjamin_franklin" /><span>Benjamin Franklin College</span></label><label class="choice-chip"><input type="radio" name="college_preference" value="no_preference" /><span>No preference</span></label></div></fieldset>
        <label class="range-field scored-range"><span><strong>What sound level do you prefer?</strong><output data-range-output="sound_level">3 — moderate</output></span><input type="range" name="sound_level" min="1" max="5" value="3" /><span class="range-ends"><small>1 — Very quiet</small><small>5 — Very loud</small></span></label>
      </section>

      <section class="survey-section" data-section="5" hidden>
        <span class="step-label">STEP 6 OF 6</span>
        <h2>Review and submit</h2>
        <p class="section-copy">Check your answers before sending them to SuiteSync.</p>
        <div class="review-list" data-review-list></div>
        <label class="check-field consent-field"><input type="checkbox" name="consent" required /><span>I consent to SuiteSync storing these responses and using them for suite matching and residential planning.</span></label>
        <div class="backend-notice" data-backend-notice hidden><strong>Supabase setup needed</strong><span>Add the public Supabase environment variables to submit this form. Your answers have not been sent.</span></div>
      </section>

      <div class="form-error" data-form-error role="alert" hidden></div>
      <div class="survey-actions">
        <button class="button secondary" type="button" data-back hidden>Back</button>
        <button class="button primary" type="button" data-next>Continue <span>→</span></button>
        <button class="button primary" type="submit" data-submit hidden>Submit survey <span>→</span></button>
      </div>
    </form>

    <section class="success-card" data-success hidden>
      <div class="success-icon">✓</div>
      <span class="step-label">RESPONSE RECEIVED</span>
      <h2>You’re one step closer to meeting your suite.</h2>
      <p>Thanks for sharing what helps you feel at home. You’ll hear from YYGS when suite matching is complete.</p>
      <a class="button primary" href={import.meta.env.BASE_URL}>Return home</a>
    </section>
  </div>
</div>

<script>
  import { createClient } from '@supabase/supabase-js';

  const root = document.querySelector('[data-survey-root]');
  if (root instanceof HTMLElement) {
    const form = root.querySelector('[data-survey-form]');
    const sections = [...root.querySelectorAll('[data-section]')];
    const progressSteps = [...root.querySelectorAll('[data-progress-step]')];
    const next = root.querySelector('[data-next]');
    const back = root.querySelector('[data-back]');
    const submit = root.querySelector('[data-submit]');
    const errorBox = root.querySelector('[data-form-error]');
    const url = root.dataset.supabaseUrl;
    const key = root.dataset.supabaseKey;
    const supabase = url && key ? createClient(url, key) : null;
    let current = 0;

    const showError = (message = '') => {
      if (!(errorBox instanceof HTMLElement)) return;
      errorBox.textContent = message;
      errorBox.hidden = !message;
    };

    const validateSection = () => {
      const section = sections[current];
      if (!(section instanceof HTMLElement)) return false;
      const fields = [...section.querySelectorAll('input, select, textarea')];
      for (const field of fields) {
        if (field instanceof HTMLInputElement || field instanceof HTMLSelectElement || field instanceof HTMLTextAreaElement) {
          if (!field.checkValidity()) {
            field.reportValidity();
            return false;
          }
        }
      }
      showError();
      return true;
    };

    const updateReview = () => {
      if (!(form instanceof HTMLFormElement)) return;
      const data = new FormData(form);
      const labels = {
        preferred_name: 'Preferred name', email: 'Application email', track: 'YYGS track', session: 'YYGS session', age: 'Age', biological_sex: 'Biological sex', pronouns: 'Pronouns', gender_identity: 'Gender identity', ethnicity: 'Ethnicity', religious_belief: 'Religious belief',
        extroversion: 'Extroversion', organization: 'Organization', room_type: 'Room preference', bedtime_preference: 'Usual bedtime', preferred_suitemates: 'Preferred number of suitemates',
        floor_preference: 'Floor preference', college_preference: 'Residential college preference', sound_level: 'Preferred sound level'
      };
      const review = root.querySelector('[data-review-list]');
      if (!(review instanceof HTMLElement)) return;
      review.innerHTML = Object.entries(labels).map(([key, label]) => {
        const values = data.getAll(key).filter(Boolean).map(String);
        return `<div><span>${label}</span><strong>${values.length ? values.join(', ') : 'Not provided'}</strong></div>`;
      }).join('');
    };

    const renderStep = () => {
      sections.forEach((section, index) => {
        if (section instanceof HTMLElement) {
          section.hidden = index !== current;
          section.classList.toggle('is-active', index === current);
        }
      });
      progressSteps.forEach((step, index) => {
        step.classList.toggle('is-active', index === current);
        step.classList.toggle('is-complete', index < current);
      });
      const label = root.querySelector('[data-progress-label]');
      const bar = root.querySelector('[data-progress-bar]');
      if (label) label.textContent = `${current + 1} of ${sections.length}`;
      if (bar instanceof HTMLElement) bar.style.width = `${((current + 1) / sections.length) * 100}%`;
      if (back instanceof HTMLElement) back.hidden = current === 0;
      if (next instanceof HTMLElement) next.hidden = current === sections.length - 1;
      if (submit instanceof HTMLElement) submit.hidden = current !== sections.length - 1;
      if (current === sections.length - 1) updateReview();
      showError();
      root.scrollIntoView({ behavior: 'smooth', block: 'start' });
    };

    next?.addEventListener('click', () => { if (validateSection()) { current += 1; renderStep(); } });
    back?.addEventListener('click', () => { current -= 1; renderStep(); });

    root.querySelectorAll('[data-max-choices]').forEach((group) => {
      group.addEventListener('change', () => {
        const limit = Number(group.getAttribute('data-max-choices'));
        const checked = group.querySelectorAll('input:checked');
        if (checked.length > limit) {
          const last = checked[checked.length - 1];
          if (last instanceof HTMLInputElement) last.checked = false;
          showError(`Choose up to ${limit} options.`);
        } else showError();
      });
    });

    root.querySelectorAll('textarea[maxlength]').forEach((field) => {
      field.addEventListener('input', () => {
        if (!(field instanceof HTMLTextAreaElement)) return;
        const counter = root.querySelector(`[data-char-count="${field.name}"]`);
        if (counter) counter.textContent = String(field.value.length);
      });
    });

    const rangeDescriptions = {
      extroversion: ['very introverted', 'introverted', 'balanced', 'extroverted', 'very extroverted'],
      organization: ['very relaxed', 'relaxed', 'moderately organized', 'organized', 'very organized'],
      sound_level: ['very quiet', 'quiet', 'moderate', 'lively', 'very loud']
    };
    root.querySelectorAll('input[type="range"][data-range-field], input[type="range"][name="extroversion"], input[type="range"][name="organization"], input[type="range"][name="sound_level"]').forEach((range) => {
      range.addEventListener('input', () => {
        if (!(range instanceof HTMLInputElement)) return;
        const descriptions = rangeDescriptions[range.name as keyof typeof rangeDescriptions];
        const output = root.querySelector(`[data-range-output="${range.name}"]`);
        if (output && descriptions) output.textContent = `${range.value} — ${descriptions[Number(range.value) - 1]}`;
      });
    });

    form?.addEventListener('submit', async (event) => {
      event.preventDefault();
      if (!validateSection() || !(form instanceof HTMLFormElement) || !(submit instanceof HTMLButtonElement)) return;
      if (!supabase) {
        const notice = root.querySelector('[data-backend-notice]');
        if (notice instanceof HTMLElement) {
          notice.hidden = false;
        }
        return;
      }
      submit.disabled = true;
      submit.textContent = 'Submitting…';
      const data = new FormData(form);
      const response = {
        preferred_name: String(data.get('preferred_name') ?? '').trim(),
        email: String(data.get('email') ?? '').trim().toLowerCase(),
        track: String(data.get('track') ?? ''),
        session: String(data.get('session') ?? ''),
        age: Number(data.get('age')),
        biological_sex: String(data.get('biological_sex') ?? ''),
        pronouns: String(data.get('pronouns') ?? ''),
        gender_identity: String(data.get('gender_identity') ?? ''),
        ethnicity: String(data.get('ethnicity') ?? ''),
        religious_belief: String(data.get('religious_belief') ?? '') || null,
        extroversion: Number(data.get('extroversion')),
        organization: Number(data.get('organization')),
        room_type: String(data.get('room_type') ?? ''),
        bedtime_preference: String(data.get('bedtime_preference') ?? ''),
        preferred_suitemates: Number(data.get('preferred_suitemates')),
        floor_preference: String(data.get('floor_preference') ?? ''),
        college_preference: String(data.get('college_preference') ?? ''),
        sound_level: Number(data.get('sound_level')),
        consented_at: new Date().toISOString(),
        survey_version: 1
      };
      const { error } = await supabase.from('survey_responses').insert(response);
      if (error) {
        showError(error.code === '23505' ? 'A survey has already been submitted for this email.' : 'We couldn’t submit your survey. Please try again in a moment.');
        submit.disabled = false;
        submit.innerHTML = 'Submit survey <span>→</span>';
        return;
      }
      const layout = root.querySelector('.survey-layout');
      const success = root.querySelector('[data-success]');
      if (layout instanceof HTMLElement) layout.querySelectorAll(':scope > :not([data-success])').forEach((el) => { if (el instanceof HTMLElement) el.hidden = true; });
      if (success instanceof HTMLElement) success.hidden = false;
      root.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });

    renderStep();
  }
</script>
